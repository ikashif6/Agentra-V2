"use client";

import { FloatingMorphHero } from "@/components/auth/floating-morph-hero";

/**
 * Shared product visual for auth screens (login, signup, forgot password, etc.).
 */
export function AuthHeroRouter() {
  return <FloatingMorphHero />;
}
