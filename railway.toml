[build]
builder = "nixpacks"
buildCommand = "npm install && npm install --prefix widget && npm run build:widget"

[deploy]
startCommand = "npm start"
restartPolicyType = "on_failure"
restartPolicyMaxRetries = 3
