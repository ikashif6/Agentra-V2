[build]
builder = "nixpacks"
# Widget bundle is committed at widget/dist/widget.js — avoid rebuilding on Railway
# (vite is a widget devDependency and production installs can omit it).
buildCommand = "npm install"

[deploy]
startCommand = "npm start"
restartPolicyType = "on_failure"
restartPolicyMaxRetries = 3
